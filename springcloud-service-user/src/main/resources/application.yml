server:
  port: 8001

spring:
  application:
    name: jerry-service-user

eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:7001/eureka/